import { Metadata } from "next";
import React from "react"

export const metadata: Metadata = {
  title: "About",
}

const About = ({}) => {
  return (
    <h1>About</h1>
  )
};

export default About;